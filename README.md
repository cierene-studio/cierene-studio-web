# cierenestudio.com

Company website for **Cierene Studio LLC** — a single static page
(`index.html`, inline CSS, no build step) served via GitHub Pages.

## Editing

Edit `index.html` and push to `main`; GitHub Pages redeploys automatically.
The custom domain is configured via the `CNAME` file.
